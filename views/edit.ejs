<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
    <!-- jQuery library -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <!-- Popper JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <!-- Latest compiled JavaScript -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"></script>

    <style>
        input, textarea{
          border: 1px solid black;
        }
    </style>

    <title>Edit</title>
</head>
<body>
    <h1>Server Side Javascript</h1>  

    <article>
        <form action="/topic/add" method="POST" name = "add">
            <div class="form-group">
                <label for="email">제목 : </label>
                <input type="text" name="title"  class="form-control" value="<%= topic.title%>">
            </div>
            <div class="form-group">
                <label for="comment">내용 : </label>
                <textarea type="text" name="description" class="form-control" rows="5" value="<%= topic.description%>"></textarea>
            </div>
            <div class="form-group">
                <label for="email">글쓴이 : </label>
                <input type="text" name="author"  class="form-control" value="<%= topic.author%>">
            </div>
                <a href="/topic"><button type="button" class="btn btn-outline-success">수정</button></a>
                <a href="/topic"><button type="button" class="btn btn-outline-danger">취소</button></a>
        </form>
    </article>
</body>
</html>